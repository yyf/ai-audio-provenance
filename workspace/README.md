Place **your own** AI-generated or processed audio files here for analysis.

- Supported formats: WAV, MP3, M4A, AAC, FLAC, OGG
- Only analyze files you have the right to use
- Files in this directory are gitignored by default

Example workflow with Claude Desktop:

1. Copy `my-track.mp3` into this folder
2. Ask Claude to run the `analyze-ai-audio` prompt on `my-track.mp3`
